# คู่มือการติดตั้งและใช้งานระบบเช็คอุปกรณ์ประจำรถพยาบาล
## หน่วยกู้ชีพเทศบาลเมืองพันท้ายนรสิงห์ (Google Apps Script & Google Sheet)

---

### 1. ขั้นตอนการติดตั้งบน Google Sheet & Google Apps Script

#### ขั้นตอนที่ 1: สร้าง Google Sheets ใหม่
1. ไปที่ [Google Sheets](https://sheets.new) และสร้าง Spreadsheet ใหม่
2. ตั้งชื่อไฟล์ เช่น **"ระบบตรวจเช็คอุปกรณ์ประจำรถพยาบาล เทศบาลเมืองพันท้ายนรสิงห์"**

#### ขั้นตอนที่ 2: เปิดโปรแกรมแก้ไขสคริปต์ (Apps Script Editor)
1. ไปที่เมนูด้านบนของ Google Sheet: **ส่วนขยาย (Extensions)** -> **Apps Script**
2. ลบโค้ดเดิมในไฟล์ `Code.gs` ทั้งหมด แล้วคัดลอกโค้ดจากไฟล์ [Code.gs](file:///C:/Users/USER/.gemini/antigravity/scratch/ambulance-checklist-pantai/Code.gs) ไปวาง
3. คลิกปุ่ม **+ (เพิ่มไฟล์)** ทางซ้ายมือ -> เลือก **HTML** -> ตั้งชื่อว่า `index` (จะได้ไฟล์ `index.html`)
4. คัดลอกโค้ดจากไฟล์ [index.html](file:///C:/Users/USER/.gemini/antigravity/scratch/ambulance-checklist-pantai/index.html) ไปวางลงใน `index.html` ของ Apps Script ทั้งหมด
5. คลิกปุ่ม **บันทึกโครงการ (Save Project)** (ไอคอนแผ่นดิสก์)

#### ขั้นตอนที่ 3: รันคำสั่งสร้างชีทอัตโนมัติ (Init Sheets)
1. ที่แถบเครื่องมือด้านบนของ Apps Script เลือกฟังก์ชัน `initSheets`
2. คลิกปุ่ม **เรียกใช้ (Run)**
3. ระบบจะขออนุญาตการเข้าถึง (Authorization) ให้กดยอมรับสิทธิ์:
   - คลิก *ตรวจสอบสิทธิ์ (Review Permissions)*
   - เลือกบัญชี Google ของท่าน
   - คลิก *ขั้นสูง (Advanced)* -> คลิก *ไปที่... (ไม่ปลอดภัย) / Go to ... (unsafe)*
   - คลิก *อนุญาต (Allow)*
4. เมื่อรันสำเร็จ กลับไปดูที่ Google Sheet จะพบว่ามีชีทถูกสร้างขึ้นมา 6 แผ่นโดยอัตโนมัติ:
   - **กข9745** (ตารางเช็คประจำเดือน รถพยาบาลทะเบียน กข 9745 แยกเวรเช้า/เวรดึก วันที่ 1-31 รวม 65 คอลัมน์)
   - **กค7080** (ตารางเช็คประจำเดือน รถพยาบาลทะเบียน กค 7080 แยกเวรเช้า/เวรดึก วันที่ 1-31 รวม 65 คอลัมน์)
   - **กง3002** (ตารางเช็คประจำเดือน รถพยาบาลทะเบียน กง 3002 แยกเวรเช้า/เวรดึก วันที่ 1-31 รวม 65 คอลัมน์)
   - **CheckLogs** (บันทึกประวัติการเช็คทั้งหมดแบบละเอียด พร้อมระบุเวรปฏิบัติงาน)
   - **EquipmentMaster** (รายการอุปกรณ์ 40 รายการ + หมวดหมู่)
   - **Users** (บัญชีผู้ใช้งานเริ่มต้น 8 บัญชี + Admin)

#### ขั้นตอนที่ 4: เผยแพร่เว็บแอปพลิเคชัน (Deploy as Web App)
1. คลิกปุ่มสีน้ำเงินด้านขวาบน **ทำให้ใช้งานได้ (Deploy)** -> **การทำให้ใช้งานได้รายการใหม่ (New Deployment)**
2. คลิกไอคอนรูปเฟือง ⚙️ ด้านซ้าย เลือกประเภท **เว็บแอป (Web App)**
3. ตั้งค่าดังนี้:
   - **คำอธิบาย (Description)**: *EMS Panthai Checklist v4.2 (กข9745, กค7080, กง3002)*
   - **เรียกใช้ในฐานะ (Execute as)**: *ฉัน (อีเมลของคุณ / Me)*
   - **ผู้มีสิทธิ์เข้าถึง (Who has access)**: *ทุกคน (Anyone)*
4. คลิก **ทำให้ใช้งานได้ (Deploy)**
5. คัดลอก **URL เว็บแอป (Web App URL)** ไปใช้งานบนคอมพิวเตอร์ แท็บเล็ต หรือสมาร์ทโฟนของเจ้าหน้าที่ได้ทันที!

---

### 2. บัญชีผู้ใช้งานเริ่มต้นและการแสดงผลรหัสผู้ตรวจใน Sheet

| User ID | รหัสผ่านเริ่มต้น | ตำแหน่ง | **ชื่อผู้ตรวจใน Sheet** | ระดับสิทธิ์ | สถานะ | หมายเหตุ |
|---|---|---|---|---|---|---|
| `phantai312` | `123456` | **EMT** | **312** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `phantai325` | `123456` | **EMT** | **325** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `phantai326` | `123456` | **EMT** | **326** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `phantai327` | `123456` | **EMT** | **327** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `phantai122` | `123456` | **EMR** | **122** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `phantai134` | `123456` | **EMR** | **134** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `phantai136` | `123456` | **EMR** | **136** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `phantai137` | `123456` | **EMR** | **137** | Staff | อนุมัติแล้ว | บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| `ems.pantai@gmail.com` | `ems1669` | **Admin** | **1669** | Admin | อนุมัติแล้ว | สิทธิ์ผู้ดูแลระบบ |

---

### 3. โครงสร้างตารางในชีทรถพยาบาลทั้ง 3 คัน (กข9745, กค7080, กง3002)

1. **แถวที่ 1 (Title Banner)**: หัวข้อแบบบันทึกประจำรถพยาบาล (กข9745 / กค7080 / กง3002)
2. **แถวที่ 2 (Month Dropdown & Date Info)**:
   - ช่องเซลล์ B2 เป็น **Drop-down List** สำหรับเลือกเดือนภาษาไทย (มกราคม - ธันวาคม)
   - ช่องเซลล์ C2 แสดงปี พ.ศ.
   - ช่องเซลล์ D2 ถึง BM2 แสดงหัวตารางบันทึกผลประจำวัน (วันที่ 1 - 31 แยกเวรเช้า/เวรดึก)
3. **แถวที่ 3 (Day Headers)**: ผสานเซลล์ 2 คอลัมน์ต่อ 1 วัน เช่น `วันที่ 1`, `วันที่ 2`, ... `วันที่ 31`
4. **แถวที่ 4 (Shift Headers)**: ลำดับ, รายการอุปกรณ์, เกณฑ์มาตรฐาน, `เช้า`, `ดึก`, `เช้า`, `ดึก`...
5. **แถวที่ 5 ถึง 44 (Items 1-40)**: รายการอุปกรณ์ประจำรถ 40 รายการ
6. **แถวด้านล่างสุด (Bottom Row - ช่องชื่อผู้ตรวจ)**:
   - บันทึกรหัสผู้ตรวจตามวันที่และเวรที่เช็ค เช่น **312**, **325**, **122** ในช่องสีเหลืองไฮไลต์ชัดเจน
